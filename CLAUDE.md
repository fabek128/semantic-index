# Claude Code instructions

Follow the shared repository instructions in `AGENTS.md`.

Claude-specific notes:

- Keep implementations small and aligned with the documented scope.
- Do not add dependencies unless the task explicitly requires them.
- Do not read or expose `.env` values unless explicitly required.
- Update documentation when behavior changes.
- Prefer clear CLI behavior and actionable error messages.
